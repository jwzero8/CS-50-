from cs50 import get_float

while True:
  dollars = get_float("Change owned: ")
  if dollars >= 0:
    break

cents = int((dollar * 100) + 0.5)

for coins in [25, 10, 5, 1]:
  total = total + (cents / coin)
  cents = cents % coin

print(total)
